import React from 'react';
import Layer from './Layer';

const Layers = React.createClass({
  render: function(){
      return (
        <div>
          <Layer/>
        </div>
      )
  }
});

export default Layers
